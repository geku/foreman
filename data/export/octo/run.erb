#!/bin/sh
cd <%= engine.root %>
export PORT=<%= port %>
<% engine.env.each_pair do |var,env| %> export <%= var.upcase %>=<%= shell_quote(env) %>; <% end %> 
<%= process.command %>

# >> <%= log %>/<%= app %>-<%= name %>.log 2>&1'
